# Step 3.K: Stage 3 Summary

Stage 3 added flywheel-aligned analysis and reporting.

## Files changed
- `scripts/make_markov_analysis.py` — `write_interpretation()` function, captures `pipeline.run()` return
- `GrowthStoryReporter.java` — `appendIterationMotivation()`, regression flagging in `appendComparison()`
- `ExperimentApp.java` — calls `appendIterationMotivation()` in `runAllVariants()` loop

## Ready for Stage 4
Template updates: VISION-TEMPLATE.md, DESIGN-TEMPLATE.md, ROADMAP-TEMPLATE.md.
