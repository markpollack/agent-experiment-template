# Step 2.1: Config Iteration Field

Added `iteration` block to each variant in experiment-config.yaml. Control has null finding, variant-a and variant-b have placeholder findings. All three have hypotheses. YAML valid.
